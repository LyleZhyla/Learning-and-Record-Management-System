@extends($layout)
@section('title','Add Material') @section('page-title','Add Learning Material')
@section('content')
<div class="back-row"><a href="{{ route($routePrefix.'.materials.index') }}">← Back to materials</a></div><section class="card section-form-card"><div class="card-heading"><div><h3>Material details</h3><p>Upload a file up to 10 MB or provide a trusted external URL.</p></div></div><form method="POST" enctype="multipart/form-data" action="{{ route($routePrefix.'.materials.store') }}">@csrf<div class="form-grid"><label class="field-group"><span>NSTP component</span><select name="component_id" required><option value="">Select component</option>@foreach($components as $component)<option value="{{ $component->id }}" @selected(old('component_id')==$component->id)>{{ $component->code }} · {{ $component->name }}</option>@endforeach</select></label><label class="field-group"><span>Section (optional for administrators)</span><select name="section_id"><option value="">All sections in component</option>@foreach($sections as $section)<option value="{{ $section->id }}" @selected(old('section_id')==$section->id)>{{ $section->code }} · {{ $section->component->code }}</option>@endforeach</select></label><label class="field-group full"><span>Title</span><input name="title" value="{{ old('title') }}" required></label><label class="field-group full"><span>Description</span><textarea name="description" rows="4">{{ old('description') }}</textarea></label><label class="field-group"><span>Upload file</span><input type="file" name="file"></label><label class="field-group"><span>External URL</span><input type="url" name="external_url" value="{{ old('external_url') }}" placeholder="https://"></label><label class="field-group"><span>Status</span><select name="status"><option value="published">Published</option><option value="draft">Draft</option></select></label></div><div class="form-actions"><button class="primary-button compact">Save learning material</button></div></form></section>
@endsection
