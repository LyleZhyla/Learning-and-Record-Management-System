@extends($layout)
@section('title','Create Attendance') @section('page-title','Create Attendance Session')
@section('content')
<div class="back-row"><a href="{{ route($routePrefix.'.attendance.index') }}">← Back to attendance</a></div><section class="card section-form-card"><div class="card-heading"><div><span class="eyebrow">Student QR scanning</span><h3>Session details</h3><p>After saving, the facilitator can scan the permanent QR assigned to each enrolled student.</p></div></div><form method="POST" action="{{ route($routePrefix.'.attendance.store') }}" class="account-form">@csrf<div class="form-grid"><label class="field-group full"><span>Section</span><select name="section_id" required><option value="">Select a section</option>@foreach($sections as $section)<option value="{{ $section->id }}" @selected(old('section_id',$selectedSection)==$section->id)>{{ $section->code }} · {{ $section->component->code }} · {{ $section->name }}</option>@endforeach</select></label><label class="field-group full"><span>Session title</span><input name="title" value="{{ old('title') }}" placeholder="Example: Week 1 Orientation" required></label><label class="field-group"><span>Start time</span><input type="datetime-local" name="starts_at" value="{{ old('starts_at',now()->format('Y-m-d\TH:i')) }}" required></label><label class="field-group"><span>Late after</span><input type="datetime-local" name="late_after" value="{{ old('late_after',now()->addMinutes(15)->format('Y-m-d\TH:i')) }}"></label><label class="field-group"><span>End time</span><input type="datetime-local" name="ends_at" value="{{ old('ends_at',now()->addHours(2)->format('Y-m-d\TH:i')) }}" required></label></div><div class="form-actions"><button class="primary-button compact">Create attendance session</button></div></form></section>
@endsection
