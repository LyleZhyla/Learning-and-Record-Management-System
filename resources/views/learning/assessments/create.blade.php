@extends($layout)
@section('title','Create Assessment') @section('page-title','Create Assessment')
@section('content')
<div class="back-row"><a href="{{ route($routePrefix.'.assessments.index') }}">← Back to assessments</a></div><section class="card section-form-card"><div class="card-heading"><div><h3>Assessment details</h3><p>Define the points and grade weight used in automatic grade computation.</p></div></div><form method="POST" action="{{ route($routePrefix.'.assessments.store') }}">@csrf<div class="form-grid"><label class="field-group full"><span>Section</span><select name="section_id" required><option value="">Select section</option>@foreach($sections as $section)<option value="{{ $section->id }}" @selected(old('section_id')==$section->id)>{{ $section->code }} · {{ $section->component->code }}</option>@endforeach</select></label><label class="field-group full"><span>Title</span><input name="title" value="{{ old('title') }}" required></label><label class="field-group"><span>Type</span><select name="type"><option value="activity">Activity</option><option value="quiz">Quiz</option><option value="project">Project</option><option value="exam">Exam</option></select></label><label class="field-group"><span>Status</span><select name="status"><option value="published">Published</option><option value="draft">Draft</option></select></label><label class="field-group"><span>Maximum score</span><input type="number" step="0.01" min="1" name="max_score" value="{{ old('max_score',100) }}" required></label><label class="field-group"><span>Grade weight (%)</span><input type="number" step="0.01" min="0.01" max="100" name="weight" value="{{ old('weight',10) }}" required></label><label class="field-group"><span>Due date (optional)</span><input type="datetime-local" name="due_at" value="{{ old('due_at') }}"></label><label class="field-group full"><span>Instructions</span><textarea name="instructions" rows="6">{{ old('instructions') }}</textarea></label></div><div class="form-actions"><button class="primary-button compact">Create assessment</button></div></form></section>
@endsection
