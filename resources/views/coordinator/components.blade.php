@extends('layouts.coordinator')
@section('title', 'NSTP Component') @section('page-title', 'NSTP Component')
@section('content')
<div class="page-actions"><div><h2>Assigned component overview</h2><p>Read-only monitoring of the component assigned to your coordinator account.</p></div><span class="status-badge active"><i></i>Live system data</span></div><div class="management-component-grid">@forelse($components as $component)<article class="management-component-card"><div class="component-card-top"><span class="component-code-mark">{{ substr($component->code,0,1) }}</span><span class="status-badge {{ $component->is_active?'active':'inactive' }}"><i></i>{{ $component->is_active?'Active':'Inactive' }}</span></div><h3>{{ $component->code }}</h3><p>{{ $component->name }}</p><dl class="component-stat-list"><div><dt>Sections</dt><dd>{{ $component->sections_count }}</dd></div><div><dt>Enrolled students</dt><dd>{{ $component->enrollments_count }}</dd></div><div><dt>Default capacity</dt><dd>{{ $component->default_section_capacity }}</dd></div></dl><div class="coordinator-section-list">@forelse($component->sections as $section)<div><span><strong>{{ $section->code }}</strong><small>{{ $section->facilitator?->name ?? 'No facilitator assigned' }}</small></span><b>{{ $section->enrollments_count }}/{{ $section->capacity }}</b></div>@empty<p>No sections created for this component.</p>@endforelse</div></article>@empty<div class="empty-state"><strong>No component assigned</strong><span>Ask the Super Admin to assign a component to your coordinator account.</span></div>@endforelse</div>
@endsection
