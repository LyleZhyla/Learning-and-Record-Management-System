@extends('layouts.student')
@section('title','Assessments') @section('page-title','Assessments')
@section('content')
<div class="page-actions"><div><h2>Your assessments</h2><p>Submit activities and monitor grading status.</p></div></div><div class="resource-grid">@forelse($assessments as $assessment) @php($submission=$assessment->submissions->first())<article class="card resource-card"><div class="resource-meta"><span class="role-badge role-student">{{ ucfirst($assessment->type) }}</span><span class="status-badge {{ $submission?'active':'inactive' }}"><i></i>{{ $submission?($submission->score!==null?'Graded':'Submitted'):'Pending' }}</span></div><div><small>{{ $assessment->section->code }} · Due {{ $assessment->due_at?->format('M d, Y g:i A') ?? 'anytime' }}</small><h3>{{ $assessment->title }}</h3><p>{{ Str::limit($assessment->instructions,140) ?: 'Open the assessment for instructions.' }}</p></div><div class="resource-footer"><span>{{ number_format($assessment->max_score,2) }} points · {{ number_format($assessment->weight,2) }}% weight</span><a class="secondary-outline-button" href="{{ route('student.assessments.show',$assessment) }}">{{ $submission?'View submission':'Open assessment' }}</a></div></article>@empty<section class="card empty-state"><strong>No published assessments</strong><span>Activities for your section will appear here.</span></section>@endforelse</div>
@endsection
