<div class="form-grid">
    <label class="field-group full"><span>Title</span><input type="text" name="title" value="{{ old('title', $announcement?->title) }}" maxlength="180" required>@error('title')<small class="field-error">{{ $message }}</small>@enderror</label>
    <label class="field-group full"><span>Announcement</span><textarea name="body" rows="8" maxlength="10000" required>{{ old('body', $announcement?->body) }}</textarea>@error('body')<small class="field-error">{{ $message }}</small>@enderror</label>
    <label class="field-group"><span>Audience</span><select name="audience" required>@foreach($audiences as $value => $label)<option value="{{ $value }}" @selected(old('audience', $announcement?->audience ?? 'all') === $value)>{{ $label }}</option>@endforeach</select>@error('audience')<small class="field-error">{{ $message }}</small>@enderror</label>
    <label class="field-group"><span>Component</span><select name="component_id"><option value="">All components</option>@foreach($components as $component)<option value="{{ $component->id }}" @selected((int) old('component_id', $announcement?->component_id) === $component->id)>{{ $component->code }} — {{ $component->name }}</option>@endforeach</select><small class="form-help">Optional. Select one to limit the announcement to that component.</small>@error('component_id')<small class="field-error">{{ $message }}</small>@enderror</label>
    <label class="field-group"><span>Status</span><select name="status" required><option value="draft" @selected(old('status', $announcement?->status ?? 'draft') === 'draft')>Draft</option><option value="published" @selected(old('status', $announcement?->status) === 'published')>Publish now</option></select>@error('status')<small class="field-error">{{ $message }}</small>@enderror</label>
    <label class="field-group"><span>Expiration date and time</span><input type="datetime-local" name="expires_at" value="{{ old('expires_at', $announcement?->expires_at?->format('Y-m-d\TH:i')) }}"><small class="form-help">Optional. The announcement disappears automatically after this time.</small>@error('expires_at')<small class="field-error">{{ $message }}</small>@enderror</label>
</div>
